# Forge API base URL. Defaults to http://localhost:5000/api when unset,
# so local development works with no .env at all.
# For a deployed backend: VITE_API_URL=https://your-api.example.com/api
VITE_API_URL=http://localhost:5000/api
